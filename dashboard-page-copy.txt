import { useEffect, useRef, useState } from "react";

// ─── Utility ────────────────────────────────────────────────────────────────
function clsx(...args) {
  return args.filter(Boolean).join(" ");
}

// ─── Icons (inline SVG) ─────────────────────────────────────────────────────
const Icon = ({ d, d2, type = "path", ...props }) => (
  <svg
    viewBox="0 0 24 24"
    fill="none"
    stroke="currentColor"
    strokeWidth={2}
    strokeLinecap="round"
    strokeLinejoin="round"
    {...props}
  >
    {type === "path" && <path d={d} />}
    {d2 && <path d={d2} />}
  </svg>
);

// ─── Sub-components ──────────────────────────────────────────────────────────

function LiveDot() {
  return (
    <span
      style={{
        display: "inline-block",
        width: 7,
        height: 7,
        borderRadius: "50%",
        background: "#16A34A",
        animation: "glpulse 2s infinite",
      }}
    />
  );
}

function Pill({ children, color = "green" }) {
  const map = {
    green: { bg: "#DCFCE7", color: "#14532D" },
    amber: { bg: "#FEF3C7", color: "#78350F" },
    red:   { bg: "#FEE2E2", color: "#7F1D1D" },
    blue:  { bg: "#DBEAFE", color: "#1E3A8A" },
    gray:  { bg: "#F1F5F9", color: "#334155" },
  };
  const s = map[color] || map.gray;
  return (
    <span
      style={{
        display: "inline-flex",
        alignItems: "center",
        padding: "2px 9px",
        borderRadius: 20,
        fontSize: 11,
        fontWeight: 500,
        background: s.bg,
        color: s.color,
        whiteSpace: "nowrap",
      }}
    >
      {children}
    </span>
  );
}

function Avatar({ initials, color = "blue" }) {
  const map = {
    green:  { bg: "#DCFCE7", color: "#14532D" },
    blue:   { bg: "#DBEAFE", color: "#1E3A8A" },
    amber:  { bg: "#FEF3C7", color: "#78350F" },
    red:    { bg: "#FEE2E2", color: "#7F1D1D" },
    purple: { bg: "#EDE9FE", color: "#4C1D95" },
  };
  const s = map[color] || map.blue;
  return (
    <div
      style={{
        width: 30,
        height: 30,
        borderRadius: "50%",
        background: s.bg,
        color: s.color,
        fontSize: 10,
        fontWeight: 600,
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        flexShrink: 0,
      }}
    >
      {initials}
    </div>
  );
}

function Card({ children, style }) {
  return (
    <div
      style={{
        background: "#FFFFFF",
        border: "1px solid #E2E4EA",
        borderRadius: 14,
        padding: "18px",
        ...style,
      }}
    >
      {children}
    </div>
  );
}

function SectionHeader({ title, subtitle, right }) {
  return (
    <div
      style={{
        display: "flex",
        alignItems: "center",
        justifyContent: "space-between",
        marginBottom: 14,
      }}
    >
      <div>
        <div style={{ fontSize: 13, fontWeight: 600, color: "#111318" }}>{title}</div>
        {subtitle && (
          <div style={{ fontSize: 11, color: "#8B90A0", marginTop: 2 }}>{subtitle}</div>
        )}
      </div>
      {right}
    </div>
  );
}

// ─── Stats Cards ─────────────────────────────────────────────────────────────
const STATS = [
  {
    label: "Total Workers",
    value: "127",
    sub: "Registered employees",
    accentColor: "#2563EB",
    iconBg: "#DBEAFE",
    iconColor: "#2563EB",
    iconPath:
      "M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2 M17 21v-2a4 4 0 0 0-4-4 M23 21v-2a4 4 0 0 0-3-3.87 M16 3.13a4 4 0 0 1 0 7.75",
    trendPositive: null,
    trendText: null,
  },
  {
    label: "Present Today",
    value: "85",
    sub: null,
    accentColor: "#16A34A",
    iconBg: "#DCFCE7",
    iconColor: "#16A34A",
    iconPath: "M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2",
    trendPositive: true,
    trendText: "↑ +3 from yesterday",
  },
  {
    label: "Overall Efficiency",
    value: "86%",
    sub: null,
    accentColor: "#D97706",
    iconBg: "#FEF3C7",
    iconColor: "#D97706",
    iconPath: "M22 7 13.5 15.5 8.5 10.5 2 17 M16 7h6v6",
    trendPositive: false,
    trendText: "↓ −2% from average",
  },
  {
    label: "Active Alerts",
    value: "3",
    sub: "Requires immediate attention",
    subColor: "#DC2626",
    accentColor: "#DC2626",
    iconBg: "#FEE2E2",
    iconColor: "#DC2626",
    iconPath:
      "m10.29 3.86-8.6 14.9A2 2 0 0 0 3.41 22h17.18a2 2 0 0 0 1.72-3.01L13.71 3.86a2 2 0 0 0-3.42 0z M12 9v4 M12 17h.01",
    trendPositive: null,
    trendText: null,
  },
];

function StatsGrid() {
  return (
    <div
      style={{
        display: "grid",
        gridTemplateColumns: "repeat(4, minmax(0, 1fr))",
        gap: 14,
        marginBottom: 20,
      }}
    >
      {STATS.map((s) => (
        <div
          key={s.label}
          style={{
            background: "#FFFFFF",
            border: "1px solid #E2E4EA",
            borderRadius: 14,
            padding: "16px 18px",
            position: "relative",
            overflow: "hidden",
          }}
        >
          {/* Left accent stripe */}
          <div
            style={{
              position: "absolute",
              top: 0,
              left: 0,
              width: 3,
              height: "100%",
              background: s.accentColor,
              borderRadius: "3px 0 0 3px",
            }}
          />
          <div style={{ fontSize: 11, fontWeight: 500, color: "#8B90A0", textTransform: "uppercase", letterSpacing: "0.6px", marginBottom: 6 }}>
            {s.label}
          </div>
          <div style={{ fontSize: 28, fontWeight: 600, color: "#111318", letterSpacing: "-1px", lineHeight: 1.1 }}>
            {s.value}
          </div>
          <div style={{ marginTop: 6, fontSize: 12, color: "#4B5063" }}>
            {s.trendText ? (
              <span style={{ color: s.trendPositive ? "#16A34A" : "#DC2626", fontWeight: 500 }}>
                {s.trendText}
              </span>
            ) : (
              <span style={{ color: s.subColor || "#4B5063" }}>{s.sub}</span>
            )}
          </div>
          {/* Icon */}
          <div
            style={{
              position: "absolute",
              right: 16,
              top: 16,
              width: 34,
              height: 34,
              borderRadius: 8,
              background: s.iconBg,
              display: "flex",
              alignItems: "center",
              justifyContent: "center",
            }}
          >
            <svg
              viewBox="0 0 24 24"
              fill="none"
              stroke={s.iconColor}
              strokeWidth={2}
              strokeLinecap="round"
              strokeLinejoin="round"
              style={{ width: 15, height: 15 }}
            >
              {s.iconPath.split(" M ").map((seg, i) => (
                <path key={i} d={i === 0 ? seg : "M " + seg} />
              ))}
            </svg>
          </div>
        </div>
      ))}
    </div>
  );
}

// ─── Attendance Chart ─────────────────────────────────────────────────────────
function AttendanceChart() {
  const canvasRef = useRef(null);
  const chartRef = useRef(null);

  useEffect(() => {
    let Chart;
    const script = document.createElement("script");
    script.src = "https://cdnjs.cloudflare.com/ajax/libs/Chart.js/4.4.1/chart.umd.js";
    script.onload = () => {
      Chart = window.Chart;
      if (!canvasRef.current) return;
      if (chartRef.current) chartRef.current.destroy();
      chartRef.current = new Chart(canvasRef.current, {
        type: "bar",
        data: {
          labels: ["Monday", "Tuesday", "Wednesday", "Thursday", "Friday"],
          datasets: [
            {
              label: "Present",
              data: [88, 82, 91, 87, 85],
              backgroundColor: "#2563EB",
              borderRadius: 5,
              barPercentage: 0.55,
              categoryPercentage: 0.7,
            },
            {
              label: "Absent",
              data: [39, 45, 36, 40, 42],
              backgroundColor: "#E2E4EA",
              borderRadius: 5,
              barPercentage: 0.55,
              categoryPercentage: 0.7,
            },
          ],
        },
        options: {
          responsive: true,
          maintainAspectRatio: false,
          plugins: {
            legend: { display: false },
            tooltip: {
              callbacks: {
                label: (ctx) => ` ${ctx.dataset.label}: ${ctx.parsed.y} workers`,
              },
            },
          },
          scales: {
            x: {
              grid: { display: false },
              ticks: { font: { family: "'DM Sans', sans-serif", size: 11 }, color: "#8B90A0" },
            },
            y: {
              grid: { color: "#F0F1F4" },
              border: { display: false },
              ticks: {
                font: { family: "'DM Sans', sans-serif", size: 11 },
                color: "#8B90A0",
                stepSize: 20,
              },
              max: 130,
              min: 0,
            },
          },
        },
      });
    };
    document.head.appendChild(script);
    return () => {
      if (chartRef.current) chartRef.current.destroy();
    };
  }, []);

  return (
    <Card>
      <SectionHeader
        title="Weekly Attendance Overview"
        subtitle="Mon – Fri, this week"
        right={
          <div style={{ display: "flex", alignItems: "center", gap: 14 }}>
            {[
              { color: "#2563EB", label: "Present" },
              { color: "#E2E4EA", label: "Absent" },
            ].map(({ color, label }) => (
              <div key={label} style={{ display: "flex", alignItems: "center", gap: 5, fontSize: 11, color: "#4B5063" }}>
                <span style={{ width: 10, height: 10, borderRadius: 2, background: color, display: "inline-block" }} />
                {label}
              </div>
            ))}
          </div>
        }
      />
      <div style={{ position: "relative", width: "100%", height: 200 }}>
        <canvas ref={canvasRef} />
      </div>
    </Card>
  );
}

// ─── Line Assignment ──────────────────────────────────────────────────────────
const LINES = [
  { name: "Line A", dept: "Cutting",    workers: "18 / 20", eff: 91, status: "Active",   statusColor: "green" },
  { name: "Line B", dept: "Stitching",  workers: "15 / 18", eff: 83, status: "Active",   statusColor: "green" },
  { name: "Line C", dept: "Finishing",  workers: "12 / 16", eff: 75, status: "Warning",  statusColor: "amber" },
  { name: "Line D", dept: "QC / Pack",  workers: "8 / 14",  eff: 57, status: "Critical", statusColor: "red"   },
];

function effColor(v) {
  if (v >= 85) return "#16A34A";
  if (v >= 70) return "#D97706";
  return "#DC2626";
}

function LineAssignment() {
  return (
    <Card>
      <SectionHeader
        title="Production Line Summary"
        right={<span style={{ fontSize: 12, color: "#2563EB", cursor: "pointer", fontWeight: 500 }}>View all →</span>}
      />
      <table style={{ width: "100%", borderCollapse: "collapse", fontSize: 12 }}>
        <thead>
          <tr>
            {["Line", "Workers", "Efficiency", "Status"].map((h) => (
              <th
                key={h}
                style={{
                  fontSize: 11,
                  fontWeight: 500,
                  color: "#8B90A0",
                  textTransform: "uppercase",
                  letterSpacing: "0.5px",
                  padding: "6px 8px",
                  borderBottom: "1px solid #E2E4EA",
                  textAlign: "left",
                }}
              >
                {h}
              </th>
            ))}
          </tr>
        </thead>
        <tbody>
          {LINES.map((l, i) => (
            <tr key={l.name}>
              <td style={{ padding: "9px 8px", borderBottom: i < LINES.length - 1 ? "1px solid #E2E4EA" : "none", verticalAlign: "middle" }}>
                <div style={{ fontWeight: 600, color: "#111318", fontSize: 13 }}>{l.name}</div>
                <div style={{ fontSize: 10, color: "#8B90A0" }}>{l.dept}</div>
              </td>
              <td style={{ padding: "9px 8px", borderBottom: i < LINES.length - 1 ? "1px solid #E2E4EA" : "none", fontWeight: 600, color: "#111318" }}>
                {l.workers}
              </td>
              <td style={{ padding: "9px 8px", borderBottom: i < LINES.length - 1 ? "1px solid #E2E4EA" : "none" }}>
                <div style={{ fontSize: 12, fontWeight: 600, marginBottom: 4, color: "#111318" }}>{l.eff}%</div>
                <div style={{ height: 5, borderRadius: 3, background: "#F0F1F4", overflow: "hidden", width: 80 }}>
                  <div style={{ width: `${l.eff}%`, height: "100%", borderRadius: 3, background: effColor(l.eff) }} />
                </div>
              </td>
              <td style={{ padding: "9px 8px", borderBottom: i < LINES.length - 1 ? "1px solid #E2E4EA" : "none" }}>
                <Pill color={l.statusColor}>{l.status}</Pill>
              </td>
            </tr>
          ))}
        </tbody>
      </table>
    </Card>
  );
}

// ─── Live Worker Status ───────────────────────────────────────────────────────
const WORKERS = [
  { initials: "NK", name: "Nirosha Kumari",      id: "WRK-0041", line: "Line A — Cutting",    status: "On Station", statusColor: "green", avatarColor: "green", time: "08:14" },
  { initials: "SP", name: "Suneth Perera",        id: "WRK-0017", line: "Line B — Stitching",  status: "On Station", statusColor: "green", avatarColor: "blue",  time: "07:58" },
  { initials: "RM", name: "Rashmi Mendis",        id: "WRK-0033", line: "Awaiting assignment", status: "Pending",    statusColor: "amber", avatarColor: "amber", time: "09:02" },
  { initials: "KF", name: "Kasun Fernando",       id: "WRK-0058", line: "Line C — Finishing",  status: "On Station", statusColor: "green", avatarColor: "green", time: "08:05" },
  { initials: "AT", name: "Amali Thilakarathne",  id: "WRK-0072", line: "Line D — QC",         status: "Absent",     statusColor: "red",   avatarColor: "red",   time: "—" },
  { initials: "DW", name: "Dilini Wickrama",      id: "WRK-0089", line: "Line A — Cutting",    status: "On Station", statusColor: "green", avatarColor: "blue",  time: "08:22" },
];

function LiveWorkerStatus() {
  return (
    <Card style={{ marginBottom: 16 }}>
      <SectionHeader
        title="Live Worker Status"
        subtitle="Verified via face recognition & fingerprint"
        right={
          <div style={{ display: "flex", gap: 8 }}>
            <Pill color="green">85 On Station</Pill>
            <Pill color="amber">6 Pending</Pill>
            <Pill color="red">36 Absent</Pill>
          </div>
        }
      />
      <div style={{ display: "grid", gridTemplateColumns: "repeat(3, 1fr)", gap: 8 }}>
        {WORKERS.map((w) => (
          <div
            key={w.id}
            style={{
              border: "1px solid #E2E4EA",
              borderRadius: 10,
              padding: 10,
              background: "#F5F6F8",
            }}
          >
            <div style={{ display: "flex", alignItems: "center", gap: 8, marginBottom: 6 }}>
              <Avatar initials={w.initials} color={w.avatarColor} />
              <div style={{ flex: 1, minWidth: 0 }}>
                <div style={{ fontSize: 12, fontWeight: 600, color: "#111318", lineHeight: 1.2, whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }}>{w.name}</div>
                <div style={{ fontSize: 10, color: "#8B90A0", fontFamily: "monospace" }}>{w.id}</div>
              </div>
              <Pill color={w.statusColor}>{w.status}</Pill>
            </div>
            <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between" }}>
              <span style={{ fontSize: 11, color: "#4B5063" }}>{w.line}</span>
              <span style={{ fontSize: 10, color: "#8B90A0", fontFamily: "monospace" }}>{w.time}</span>
            </div>
          </div>
        ))}
      </div>
    </Card>
  );
}

// ─── Entry Validation Log ─────────────────────────────────────────────────────
const LOG_ENTRIES = [
  { initials: "NK", name: "Nirosha Kumari",    detail: "Face + Fingerprint — Gate 1", time: "08:14", status: "Validated", statusColor: "green", avatarColor: "green" },
  { initials: "SP", name: "Suneth Perera",      detail: "Face + Fingerprint — Gate 2", time: "07:58", status: "Validated", statusColor: "green", avatarColor: "blue"  },
  { initials: "??", name: "Unknown Person",     detail: "Face recognition failed — Gate 1", time: "09:11", status: "Rejected", statusColor: "red", avatarColor: "red" },
  { initials: "RM", name: "Rashmi Mendis",      detail: "Fingerprint only — Gate 3",   time: "09:02", status: "Partial",   statusColor: "amber", avatarColor: "amber" },
  { initials: "KF", name: "Kasun Fernando",     detail: "Face + Fingerprint — Gate 2", time: "08:05", status: "Validated", statusColor: "green", avatarColor: "green" },
];

function EntryValidationLog() {
  return (
    <Card>
      <SectionHeader
        title="Entry Validation Log"
        subtitle="Face recognition & fingerprint events"
        right={<span style={{ fontSize: 12, color: "#2563EB", cursor: "pointer", fontWeight: 500 }}>View all →</span>}
      />
      <div style={{ display: "flex", flexDirection: "column", gap: 8 }}>
        {LOG_ENTRIES.map((e, i) => (
          <div
            key={i}
            style={{
              display: "flex",
              alignItems: "center",
              gap: 10,
              padding: "9px 10px",
              borderRadius: 10,
              background: "#F5F6F8",
            }}
          >
            <Avatar initials={e.initials} color={e.avatarColor} />
            <div style={{ flex: 1, minWidth: 0 }}>
              <div style={{ fontSize: 12, fontWeight: 600, color: "#111318", whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }}>{e.name}</div>
              <div style={{ fontSize: 11, color: "#8B90A0" }}>{e.detail}</div>
            </div>
            <div style={{ fontSize: 11, color: "#8B90A0", fontFamily: "monospace", flexShrink: 0 }}>{e.time}</div>
            <Pill color={e.statusColor}>{e.status}</Pill>
          </div>
        ))}
      </div>
    </Card>
  );
}

// ─── Alerts Section ───────────────────────────────────────────────────────────
const ALERTS = [
  {
    severity: "critical",
    title: "Unauthorized entry attempt — Gate 1",
    desc: "Face recognition returned no match. Manual review required.",
    time: "Today, 09:11",
  },
  {
    severity: "critical",
    title: "Line D understaffed — 8 of 14 workers present",
    desc: "QC / Packing efficiency critically low at 57%. Reassignment needed.",
    time: "Today, 08:30",
  },
  {
    severity: "warning",
    title: "Line C efficiency below target",
    desc: "Currently at 75%, target is 85%. Monitor closely this shift.",
    time: "Today, 09:00",
  },
  {
    severity: "info",
    title: "Partial verification — Rashmi Mendis",
    desc: "Only fingerprint captured at Gate 3. Face scan pending.",
    time: "Today, 09:02",
  },
];

const ALERT_STYLES = {
  critical: { bg: "#FFF5F5", border: "#DC2626", iconBg: "#FEE2E2", iconColor: "#DC2626" },
  warning:  { bg: "#FFFBEB", border: "#D97706", iconBg: "#FEF3C7", iconColor: "#D97706" },
  info:     { bg: "#EFF6FF", border: "#2563EB", iconBg: "#DBEAFE", iconColor: "#2563EB" },
};

function AlertIcon({ severity }) {
  const color = ALERT_STYLES[severity].iconColor;
  return (
    <svg viewBox="0 0 24 24" fill="none" stroke={color} strokeWidth={2} strokeLinecap="round" strokeLinejoin="round" style={{ width: 14, height: 14 }}>
      {severity === "info" ? (
        <>
          <circle cx="12" cy="12" r="10" />
          <line x1="12" y1="16" x2="12" y2="12" />
          <line x1="12" y1="8" x2="12.01" y2="8" />
        </>
      ) : (
        <>
          <path d="m10.29 3.86-8.6 14.9A2 2 0 0 0 3.41 22h17.18a2 2 0 0 0 1.72-3.01L13.71 3.86a2 2 0 0 0-3.42 0z" />
          <line x1="12" y1="9" x2="12" y2="13" />
          <line x1="12" y1="17" x2="12.01" y2="17" />
        </>
      )}
    </svg>
  );
}

function AlertsSection() {
  return (
    <Card>
      <SectionHeader
        title="Active Alerts"
        right={<Pill color="red">3 Unresolved</Pill>}
      />
      <div style={{ display: "flex", flexDirection: "column", gap: 8 }}>
        {ALERTS.map((a, i) => {
          const s = ALERT_STYLES[a.severity];
          return (
            <div
              key={i}
              style={{
                display: "flex",
                gap: 10,
                padding: "10px 12px",
                borderRadius: 10,
                background: s.bg,
                borderLeft: `3px solid ${s.border}`,
              }}
            >
              <div
                style={{
                  width: 28,
                  height: 28,
                  borderRadius: 6,
                  background: s.iconBg,
                  display: "flex",
                  alignItems: "center",
                  justifyContent: "center",
                  flexShrink: 0,
                }}
              >
                <AlertIcon severity={a.severity} />
              </div>
              <div style={{ flex: 1 }}>
                <div style={{ fontSize: 12, fontWeight: 600, color: "#111318", marginBottom: 1 }}>{a.title}</div>
                <div style={{ fontSize: 11, color: "#4B5063", lineHeight: 1.4 }}>{a.desc}</div>
                <div style={{ fontSize: 10, color: "#8B90A0", fontFamily: "monospace", marginTop: 3 }}>{a.time}</div>
              </div>
            </div>
          );
        })}
      </div>
    </Card>
  );
}

// ─── Clock Hook ───────────────────────────────────────────────────────────────
function useClock() {
  const [time, setTime] = useState(() =>
    new Date().toLocaleTimeString("en-GB", { hour: "2-digit", minute: "2-digit", second: "2-digit" })
  );
  useEffect(() => {
    const id = setInterval(
      () =>
        setTime(
          new Date().toLocaleTimeString("en-GB", { hour: "2-digit", minute: "2-digit", second: "2-digit" })
        ),
      1000
    );
    return () => clearInterval(id);
  }, []);
  return time;
}

// ─── Main Dashboard Page ──────────────────────────────────────────────────────
export function DashboardPage() {
  const clock = useClock();

  return (
    <>
      {/* Global styles injected once */}
      <style>{`
        @import url('https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600&family=DM+Mono:wght@400;500&display=swap');
        * { box-sizing: border-box; margin: 0; padding: 0; }
        body { font-family: 'DM Sans', system-ui, sans-serif; background: #F5F6F8; color: #111318; }
        @keyframes glpulse { 0%,100% { opacity: 1; } 50% { opacity: 0.4; } }
      `}</style>

      <div style={{ minHeight: "100vh", background: "#F5F6F8", fontFamily: "'DM Sans', system-ui, sans-serif" }}>
        {/* Top Bar */}
        <div
          style={{
            display: "flex",
            alignItems: "center",
            justifyContent: "space-between",
            padding: "14px 24px",
            background: "#FFFFFF",
            borderBottom: "1px solid #E2E4EA",
            position: "sticky",
            top: 0,
            zIndex: 100,
          }}
        >
          <div style={{ display: "flex", alignItems: "center", gap: 14 }}>
            <div style={{ fontSize: 15, fontWeight: 600, color: "#111318", letterSpacing: "-0.2px" }}>
              Line<span style={{ color: "#2563EB" }}>Matrix</span> — Operations Centre
            </div>
            <div style={{ width: 1, height: 20, background: "#CACDD8" }} />
            <div style={{ fontSize: 13, fontWeight: 500, color: "#4B5063" }}>Dashboard Overview</div>
          </div>
          <div style={{ display: "flex", alignItems: "center", gap: 10 }}>
            <div
              style={{
                display: "flex",
                alignItems: "center",
                gap: 6,
                padding: "5px 10px",
                borderRadius: 20,
                background: "#DCFCE7",
                fontSize: 12,
                fontWeight: 500,
                color: "#14532D",
              }}
            >
              <LiveDot /> Live
            </div>
            <div style={{ fontSize: 12, color: "#8B90A0", fontFamily: "monospace" }}>{clock}</div>
            <button
              style={{
                padding: "6px 12px",
                borderRadius: 10,
                border: "1px solid #CACDD8",
                background: "#FFFFFF",
                fontSize: 12,
                fontWeight: 500,
                color: "#4B5063",
                cursor: "pointer",
              }}
            >
              Export Report
            </button>
          </div>
        </div>

        {/* Page Content */}
        <div style={{ padding: "20px 24px 32px" }}>
          <StatsGrid />

          {/* Main 2-col grid */}
          <div
            style={{
              display: "grid",
              gridTemplateColumns: "minmax(0, 2fr) minmax(0, 1fr)",
              gap: 16,
              marginBottom: 16,
            }}
          >
            <AttendanceChart />
            <LineAssignment />
          </div>

          <LiveWorkerStatus />

          {/* Bottom 2-col grid */}
          <div
            style={{
              display: "grid",
              gridTemplateColumns: "minmax(0, 1fr) minmax(0, 1fr)",
              gap: 16,
            }}
          >
            <EntryValidationLog />
            <AlertsSection />
          </div>
        </div>
      </div>
    </>
  );
}

export default DashboardPage;
